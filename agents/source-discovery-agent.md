# Source Discovery Agent

Pesquisa candidatos somente por API de busca autorizada. Normaliza o domínio e compara com `data/sources.json`. Cria candidatos como `pending_review`; nunca ativa, coleta, contorna CAPTCHA ou usa login.
